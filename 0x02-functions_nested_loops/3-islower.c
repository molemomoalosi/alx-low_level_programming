#include "main.h"
#include <stdio.h>

/**
 * _islower - tests whether char is lower
 * @r: tests is lower
 * @c: character tested
 *
 * Return: 1 or 0
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
